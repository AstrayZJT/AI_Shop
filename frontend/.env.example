# Optional: if omitted, Vite will probe localhost/127.0.0.1 on 8080 and 8082,
# then pick the first reachable Spring Boot API automatically.
VITE_BACKEND_TARGET=http://localhost:8080
VITE_FRONTEND_PORT=5173
VITE_FRONTEND_PREVIEW_PORT=4173
